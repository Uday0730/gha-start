name: Hello world
on: workflow_dispatch
jobs:
  build:
   runs-on: ubuntu-latest
   steps:
     - name: Build step
       run: echo "A demo run"
  test:
    runs-on: ubuntu-latest
    steps:
      - name: test step
        run: echo"performing test cases"
  deploy:
    runs-on: ubuntu-latest
    steps:
     - name: deploy step
       run: echo"deploying application"
     
          
        
