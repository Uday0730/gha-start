name: Hello world
on: 
  push: 
    branches:
     -main
     -develop
     -'release/**'
  pull_request:
    branches:
      - -main   
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
     - name: Build step
       run: echo "Hello, ${{ vars.MY_NAME}}"
 



     
          
        
